apply plugin: 'com.android.application'

android {
    signingConfigs {
        Pinyto {
            keyAlias 'pinyto-key'
            keyPassword 'aQqQm6xUxi'
            storeFile file('exalteddicer.jks')
            storePassword 'aQqQm6xUxi'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "de.pinyto.exalteddicer"
        minSdkVersion 16
        targetSdkVersion 21
        versionCode 4
        versionName '2.2'
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.Pinyto
        }
    }
    productFlavors {
        ExaltedDicer {
            signingConfig signingConfigs.Pinyto
            versionCode 4
            versionName '2.2'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:21.0.3'
}
